export type APIRepository = {
  id: number;
  name: string;
  html_url: string;
  description: string;
};

export type Repository = {
  id: number;
  name: string;
  url: string;
  description: string;
};
